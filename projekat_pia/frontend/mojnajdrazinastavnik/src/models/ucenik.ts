import { ScheduledClassStudent } from "./ScheduledClassStudent";

export class Ucenik{

    
    username:string;
    school:string;
    grade:number;
    scheduledClasses:Array<ScheduledClassStudent>=[];
    avg:number=0;
    studentgrades:Array<number>=[];
   
    
}